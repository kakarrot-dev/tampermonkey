# Claude Code 项目入口

请先读取并遵守：

@AGENTS.md

补充规则：

- 默认使用简体中文沟通。
- 不要在终端展示大段代码或完整 diff，除非用户明确要求。
- 中大型任务先按 AGENTS.md 编写 Spec 和 Plan，确认后再实现。
- `README.md` 是英文主入口，`README.zh-CN.md` 是简体中文版本；公开信息变更时同步维护两份文档。
- `X/` 和 `2libra/` 是自有脚本；`V2EX/` 是第三方参考快照，除非任务明确要求，否则不要修改或复用其许可证声明。
- 仓库没有构建链路。验证以脚本语法检查、metadata 检查和目标站点手动验证为准。
- 视觉资产放在 `assets/`，README 使用 Claude Cream 配色资源，不提交临时截图或生成中间文件。
